version: '5.0.2'

services:
  redis:
    image: redis
    command: redis-server  --requirepass EOM*191721*UNR --appendonly yes
    volumes:
      - redis_data:/data
    ports:
      - "6379:6379"
volumes:
  redis_data:
